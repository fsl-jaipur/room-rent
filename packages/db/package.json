{
  "name": "@rent/db",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate --schema ./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "@rent/config": "file:../config",
    "@rent/shared": "file:../shared"
  },
  "devDependencies": {
    "prisma": "^6.6.0",
    "typescript": "^5.6.3"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma"
  }
}
